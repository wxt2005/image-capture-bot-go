telegram:
  bot_token:
  channel_name:
  auth_key:

twitter:
  bearer_token:
  auth_token:

dropbox:
  access_token:
  save_path: "/test"

pixiv:
  username:
  password:
  client_id:
  client_secret:
  initial_access_token:
  initial_refresh_token:

db:
  db_path: ./external/bot.db
  url_bucket: url
  like_bucket: like
  auth_bucket: auth

danbooru:
  username:
  key: 

s3:
  bucket:
  save_path: "."
  access_key_id:
  secret_access_key:
